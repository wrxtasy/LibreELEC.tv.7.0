commit ae2c489b5191707ac9a6674be31ab034dcc271fd
Author: Jamie Coldhill <wrxtasy@amnet.net.au>
Date:   Thu Jun 2 02:27:11 2016 +0800

    3D: Fix RW Permissions for ppmgr_class_attrs

diff --git a/drivers/amlogic/ppmgr/ppmgr_drv.c b/drivers/amlogic/ppmgr/ppmgr_drv.c
index 984469b..0130dc6 100644
--- a/drivers/amlogic/ppmgr/ppmgr_drv.c
+++ b/drivers/amlogic/ppmgr/ppmgr_drv.c
@@ -1032,43 +1032,43 @@ static ssize_t ppmgr_vframe_states_show(struct class *cla,
 
 static struct class_attribute ppmgr_class_attrs[] = {
 __ATTR(info,
-	S_IRUGO | S_IWUSR,
+	0666,
 	show_ppmgr_info,
 	NULL),
 __ATTR(angle,
-	S_IRUGO | S_IWUSR | S_IWGRP,
+	0666,
 	angle_read,
 	angle_write),
 __ATTR(rect,
-	S_IRUGO | S_IWUSR,
+	0666,
 	rect_read,
 	rect_write),
 __ATTR(bypass,
-	S_IRUGO | S_IWUSR,
+	0666,
 	bypass_read,
 	bypass_write),
 
 __ATTR(disp,
-	S_IRUGO | S_IWUSR | S_IWGRP,
+	0666,
 	disp_read,
 	disp_write),
 
 __ATTR(orientation,
-	S_IRUGO | S_IWUSR,
+	0666,
 	orientation_read,
 	orientation_write),
 #ifdef CONFIG_POST_PROCESS_MANAGER_PPSCALER
 	__ATTR(ppscaler,
-		S_IRUGO | S_IWUSR | S_IWGRP,
+		0666,
 		ppscaler_read,
 		ppscaler_write),
 	__ATTR(ppscaler_rect,
-		S_IRUGO | S_IWUSR | S_IWGRP,
+		0666,
 		ppscaler_rect_read,
 		ppscaler_rect_write),
 #endif
 	__ATTR(vtarget,
-		S_IRUGO | S_IWUSR | S_IWGRP,
+		0666,
 		receiver_read,
 		receiver_write),
 #ifdef CONFIG_POST_PROCESS_MANAGER_3D_PROCESS
@@ -1077,59 +1077,59 @@ __ATTR(orientation,
 		_3dmode_read,
 		_3dmode_write),
 	__ATTR(viewmode,
-		S_IRUGO | S_IWUSR,
+		0666,
 		viewmode_read,
 		viewmode_write),
 	__ATTR(doublemode,
-		S_IRUGO | S_IWUSR,
+		0666,
 		doublemode_read,
 		doublemode_write),
 	__ATTR(switchmode,
-		S_IRUGO | S_IWUSR,
+		0666,
 		switchmode_read,
 		switchmode_write),
 	__ATTR(direction_3d,
-		S_IRUGO | S_IWUSR,
+		0666,
 		direction_3d_read,
 		direction_3d_write),
 	__ATTR(scale_down,
-		S_IRUGO | S_IWUSR,
+		0666,
 		scale_down_read,
 		scale_down_write),
 	__ATTR(depth,
-		S_IRUGO | S_IWUSR,
+		0666,
 		read_depth,
 		write_depth),
 	__ATTR(view_mode,
-		S_IRUGO | S_IWUSR,
+		0666,
 		read_view_mode,
 		write_view_mode),
 	__ATTR(vertical_sample,
-		S_IRUGO | S_IWUSR,
+		0666,
 		read_vertical_sample,
 		write_vertical_sample),
 	__ATTR(scale_width,
-		S_IRUGO | S_IWUSR,
+		0666,
 		read_scale_width,
 		write_scale_width),
 	__ATTR(axis,
-		S_IRUGO | S_IWUSR,
+		0666,
 		cut_win_show,
 		cut_win_store),
 #endif
 
 	__ATTR(platform_type,
-		S_IRUGO | S_IWUSR,
+		0666,
 		platform_type_read,
 		platform_type_write),
 
 	__ATTR(mirror,
-		S_IRUGO | S_IWUSR,
+		0666,
 		mirror_read,
 		mirror_write),
 	__ATTR_RO(ppmgr_vframe_states),
 	__ATTR(disable_prot,
-		S_IRUGO | S_IWUSR,
+		0666,
 		disable_prot_show,
 		disable_prot_store),
 	__ATTR_NULL };
